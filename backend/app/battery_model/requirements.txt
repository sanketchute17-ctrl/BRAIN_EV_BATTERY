# Battery Digital Twin — Python Backend Dependencies
# ================================================
# Install with: pip install -r requirements.txt

# ── Required for REST API server ─────────────────────────────────────────────
fastapi>=0.111.0          # REST API framework
uvicorn[standard]>=0.29.0 # ASGI server for FastAPI
pydantic>=2.7.0           # Data validation for API models

# ── Required for dataset loading ─────────────────────────────────────────────
scipy>=1.13.0             # NASA .mat file loading (scipy.io.loadmat)
numpy>=1.26.0             # Numerical arrays for physics calculations

# ── Required for validation metrics ──────────────────────────────────────────
# (numpy already covers RMSE/MAE calculations)

# ── Required for AI model inference (optional, install when models are ready) ─
scikit-learn>=1.4.0       # ML model loading (.pkl), feature pipelines
# xgboost>=2.0.0          # Uncomment if XGBoost models are used
# lightgbm>=4.0.0         # Uncomment if LightGBM models are used

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dateutil>=2.9.0    # Date/time utilities
